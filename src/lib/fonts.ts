import { DynaPuff, Figtree } from 'next/font/google';

export const figtree = Figtree({ subsets: ['latin'], weight: ['400'] });

export const dynaPuff = DynaPuff({ subsets: ['latin'], weight: ['400'] });
